api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]

    # Set name of custom filter parameter here
    #    collection:
    #        order_parameter_name: "_order"
